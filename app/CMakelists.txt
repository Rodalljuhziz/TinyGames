set(TARGET_NAME ${CMAKE_PROJECT_NAME})

add_executable(${TARGET_NAME}
)

target_sources(${TARGET_NAME} PRIVATE
    main.cpp
    TinyGamesLauncher.h
    TinyGamesLauncher.cpp
)

target_link_libraries(${TARGET_NAME} PUBLIC
    TinyGamesConsoleUI
    TinyGamesQtUI
    TinyGamesSDLUI
    glog::glog
    SDL3::SDL3
    Qt6::Core
    Qt6::Gui
    Qt6::Widgets
)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)


set(MINGW_DLL_DIR "C:/Program Files/JetBrains/CLion 2024.1.5/bin/mingw/bin")

set(MINGW_DLLS
        "libwinpthread-1.dll"
        "libstdc++-6.dll"
        "libgcc_s_seh-1.dll"
)

function(copy_mingw_dlls target_name)
    foreach(config IN ITEMS Debug Release)
        foreach(dll IN LISTS MINGW_DLLS)
            add_custom_command(TARGET ${target_name} POST_BUILD
                    COMMAND ${CMAKE_COMMAND} -E copy_if_different
                    "${MINGW_DLL_DIR}/${dll}"
                    "$<TARGET_FILE_DIR:${target_name}>/${dll}"
                    COMMENT "Copying ${dll} to ${config} output directory"
            )
        endforeach()
    endforeach()
endfunction()

set(WINDEPLOYQT_EXECUTABLE "C:/Qt/6.9.1/mingw_64/bin/windeployqt.exe")

add_custom_command(TARGET ${TARGET_NAME} POST_BUILD
        COMMAND "${WINDEPLOYQT_EXECUTABLE}"
        "$<TARGET_FILE:${TARGET_NAME}>"
        COMMENT "Running windeployqt on ${TARGET_NAME}"
)

copy_mingw_dlls(${TARGET_NAME})